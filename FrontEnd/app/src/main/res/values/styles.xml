<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Style cho text của menu items  -->
    <style name="MenuTextAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">normal</item>
    </style>

    <!-- style riêng cho title (Other features, Account)-->
    <style name="MenuTitleAppearance" parent="TextAppearance.AppCompat">
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/red_title</item>
    </style>

    <!-- Pagination Button Style -->
    <style name="PaginationButtonStyle" parent="Widget.Material3.Button.OutlinedButton">
        <item name="android:layout_width">30dp</item>
        <item name="android:layout_height">30dp</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:background">@drawable/pagination_button_background</item>
        <item name="android:minWidth">30dp</item>
        <item name="android:minHeight">30dp</item>
        <item name="android:paddingHorizontal">6dp</item>
        <item name="android:paddingVertical">6dp</item>
        <item name="android:elevation">2dp</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="rippleColor">@color/primary_color_light</item>
    </style>

    <!-- Page Number Button Style -->
    <style name="PageNumberButtonStyle" parent="PaginationButtonStyle">
        <item name="android:layout_width">30dp</item>
        <item name="android:layout_height">30dp</item>
        <item name="android:textSize">10sp</item>
        <item name="android:minWidth">30dp</item>
        <item name="android:minHeight">30dp</item>
        <item name="android:paddingHorizontal">3dp</item>
        <item name="android:paddingVertical">3dp</item>
    </style>
</resources>
